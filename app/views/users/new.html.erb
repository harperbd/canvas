<h1>Users Form</h1>

<% form_for @user do |u| %>
  <%= u.label :first_name %>
  <%= u.text_field :first_name, required: true %>

  <%= u.label :last_name %>
  <%= u.text_field :last_name %>

  <%= u.submit %>
<% end %>
